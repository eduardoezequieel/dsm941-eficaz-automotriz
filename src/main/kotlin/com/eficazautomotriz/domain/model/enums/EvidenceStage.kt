package com.eficazautomotriz.domain.model.enums

/** Etapa del servicio en la que se capturo la evidencia fotografica. */
enum class EvidenceStage(val label: String) {
    RECEPTION("Recepcion"),
    DIAGNOSIS("Diagnostico"),
    RESULT("Resultado"),
}